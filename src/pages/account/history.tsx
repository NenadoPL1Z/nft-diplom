import React from "react";
import AccountWrapper from "@/components/Wrappers/AccountWrapper/AccountWrapper";

const History = () => {
  return (
    <AccountWrapper title="История">
      <h1>ok</h1>
    </AccountWrapper>
  );
};

export default React.memo(History);
