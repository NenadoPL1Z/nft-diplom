import React from "react";
import AccountWrapper from "@/components/Wrappers/AccountWrapper/AccountWrapper";

const Exchange = () => {
  return (
    <AccountWrapper title="Предпочтительная биржа">
      <h1>ok</h1>
    </AccountWrapper>
  );
};

export default React.memo(Exchange);
